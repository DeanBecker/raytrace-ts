{
    "scripts": {
        "prestart": "npm run build",
        "test": "echo \"Error: no tests\" && exit 1",
        "build": "tsc",
        "start": "node app/js/server.js"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^10.12.18"
    }
}
